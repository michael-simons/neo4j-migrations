// Right at the start
  // in the middle
MATCH (n)
// inside a statement which we will ignore
RETURN count(n) AS n;
MATCH (n)
RETURN count(n) AS n;
MATCH (n)
RETURN count(n)
  // this too
AS n;
//the end
